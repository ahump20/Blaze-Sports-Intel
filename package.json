{
  "name": "blaze-sports-intel",
  "private": true,
  "version": "4.0.0",
  "packageManager": "pnpm@9.6.0",
  "workspaces": ["apps/*", "workers/*", "packages/*"],
  "scripts": {
    "dev:mobile": "pnpm --filter @blaze/mobile start",
    "dev:api": "pnpm --filter @blaze/worker-api dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
